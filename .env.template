# Tradov environment template.
#
# This file is an alias for .env.example so both historical references work.
# To create a new .env, copy .env.example (the authoritative list of keys) and
# fill in values. Do NOT add new variables here — add them to .env.example.
#
# cp .env.example .env
