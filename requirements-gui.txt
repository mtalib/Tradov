# GUI Dependencies (Optional)
# Primary GUI Framework
PySide6>=6.5.0

# Authentication and Security
PyJWT>=2.8.0
cryptography>=41.0.0
requests>=2.31.0

# Visualization Libraries (Plotly is primary, matplotlib as fallback)
plotly>=5.17.0
dash>=2.14.0
kaleido>=0.2.1  # For static image export in Plotly

# Legacy/Fallback Visualization (optional)
matplotlib>=3.5.0
seaborn>=0.11.0

# Additional GUI Components
pyqtgraph>=0.12.0
qasync>=0.27.0  # Required: asyncio/PySide6 event loop integration (Linux/Wayland)

# WebEngine for Plotly embedding
# Note: PySide6 6.5+ includes QtWebEngine automatically
# No separate qtwebengine package needed for pip
