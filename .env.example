# ==============================================================================
# SPYDER TRADING SYSTEM - Environment Configuration Template
# ==============================================================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual API keys and credentials
# 3. NEVER commit the .env file to git (it's in .gitignore)
# 4. Start with TRADING_MODE=sandbox for testing
#
# ==============================================================================

# ==============================================================================
# TRADIER API CONFIGURATION (Order Execution + optional Market Data)
# ==============================================================================
# Get your API key from: https://brokerage.tradier.com/
# Navigate to: Account > API Access
#
# Paper/sandbox account (sandbox.tradier.com) — used when TRADING_MODE=sandbox|paper
TRADIER_SANDBOX_API_KEY=your_tradier_sandbox_api_key_here
TRADIER_SANDBOX_ACCOUNT_ID=your_tradier_sandbox_account_id_here
#
# Live/production account (api.tradier.com) — used when TRADING_MODE=live
TRADIER_LIVE_API_KEY=your_tradier_live_api_key_here
TRADIER_LIVE_ACCOUNT_ID=your_tradier_live_account_id_here
#
# Legacy single-pair fallback (used if the environment-specific vars above are absent)
TRADIER_API_KEY=your_tradier_api_key_here
TRADIER_ACCOUNT_ID=your_tradier_account_id_here

# Tradier URLs (usually don't need to change these)
TRADIER_SANDBOX_URL=https://sandbox.tradier.com/v1
TRADIER_LIVE_URL=https://api.tradier.com/v1

# ==============================================================================
# MASSIVE CONFIGURATION (Market Data — live and paper trading)
# ==============================================================================
MASSIVE_API_KEY=your_massive_api_key_here
MASSIVE_UNDERLYINGS=SPY
MASSIVE_STREAM_TRADES=true
MASSIVE_STREAM_QUOTES=true
MASSIVE_REST_RPS=5

# ==============================================================================
# TRADING MODE CONFIGURATION
# ==============================================================================
# CRITICAL: Always start with 'sandbox' for testing
# Options: sandbox, paper, live
TRADING_MODE=sandbox

# Data Provider (massive = default; tradier = use Tradier quotes/options as data source)
# Use DATA_PROVIDER=tradier for paper trading without a Massive API key.
DATA_PROVIDER=massive

# Execution Provider (currently only tradier supported)
EXECUTION_PROVIDER=tradier

# ==============================================================================
# LIVE TRADING SAFETY
# ==============================================================================
# IMPORTANT: These settings prevent accidental live trading
# Set to 'true' only when you understand the risks
REQUIRE_LIVE_CONFIRMATION=true
LIVE_TRADING_CONFIRMED=false

# ==============================================================================
# ORDER CONFIRMATION SETTINGS
# ==============================================================================
# REQUIRE_LIVE_ORDER_CONFIRMATION: Development mode flag
#   - false (default): Autonomous operation, no blocking confirmation
#   - true: Development mode - ALL orders require confirmation before execution
REQUIRE_LIVE_ORDER_CONFIRMATION=false

# HIGH_RISK_ORDER_CONFIRMATION: Selective confirmation for exceptional orders
#   - true (default): Require confirmation for orders exceeding thresholds
#   - false: Fully autonomous, never require confirmation (use with caution)
HIGH_RISK_ORDER_CONFIRMATION=true

# HIGH_RISK_ORDER_THRESHOLD_USD: Dollar threshold for high-risk orders
#   - Orders >= this value require confirmation (if HIGH_RISK_ORDER_CONFIRMATION=true)
#   - Default: $50,000
HIGH_RISK_ORDER_THRESHOLD_USD=50000

# HIGH_RISK_ORDER_PORTFOLIO_PCT: Portfolio percentage threshold
#   - Orders >= this % of portfolio require confirmation
#   - Default: 0.25 (25% of portfolio)
HIGH_RISK_ORDER_PORTFOLIO_PCT=0.25

# AUTO_CONFIRM_HIGH_RISK_ORDERS: Testing/emergency override
#   - false (default): High-risk orders require actual user confirmation
#   - true: Auto-approve high-risk orders (TESTING ONLY - dangerous in production)
AUTO_CONFIRM_HIGH_RISK_ORDERS=false

# Trading Risk Limits
MAX_DAILY_TRADES=100
MAX_POSITION_SIZE=10000
MAX_DAILY_LOSS_USD=10000
EMERGENCY_STOP_LOSS_PCT=0.05

# CLOSE_POSITIONS_ON_EMERGENCY: What happens when an emergency stop fires.
#   - false (DEFAULT): Emergency stop halts all new entries but leaves existing
#     positions open for manual management.  Preferred when you want human review
#     before closing, or your broker requires explicit close orders per position.
#   - true: Emergency stop submits market-close orders for every open position.
#     Use in fully autonomous, unattended deployments where open exposure during
#     an outage is unacceptable.
# NOTE: API Panic Mode (3 consecutive Tradier 5xx errors) also triggers an
#       emergency stop — with the default 'false', positions remain open if
#       Tradier's API goes down.  Set to 'true' if you prefer auto-close.
CLOSE_POSITIONS_ON_EMERGENCY=false

# ==============================================================================
# NOTIFICATIONS (Optional)
# ==============================================================================
# Telegram Bot (optional - for trade alerts)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Email Alerts (optional)
EMAIL_ADDRESS=
EMAIL_PASSWORD=
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# ==============================================================================
# SYSTEM CONFIGURATION
# ==============================================================================
# Logging
LOG_LEVEL=INFO
GUI_LOG_LEVEL=INFO

# Debug Mode (set to true for detailed logging)
DEBUG_MODE=false

# ==============================================================================
# DATABASE (Optional - uses defaults if not set)
# ==============================================================================
# DATABASE_PATH=data/spyder.db

# ==============================================================================
# OLLAMA CONFIGURATION (Local LLM Agents)
# ==============================================================================
# Ollama base URL (default: http://localhost:11434)
OLLAMA_BASE_URL=http://localhost:11434

# Model roles — set model names per role
#
# Gemma 4 (RECOMMENDED — Apache 2.0, available in Ollama, superior benchmarks):
#   ollama pull gemma4:e4b   (9.6 GB, 128K ctx — FAST/FINANCE roles)
#   ollama pull gemma4:26b   (18 GB,  256K ctx — PRIMARY/CODE roles, MoE 4B active)
#   See: 06-Research/gemma4-spyder-analysis.md
#
OLLAMA_PRIMARY_MODEL=gemma4:26b
OLLAMA_FAST_MODEL=gemma4:e4b
OLLAMA_CODE_MODEL=gemma4:26b
OLLAMA_FINANCE_MODEL=gemma4:e4b
#
# Legacy / fallback (if Gemma 4 not yet pulled):
# OLLAMA_PRIMARY_MODEL=llama3.1:70b
# OLLAMA_FAST_MODEL=llama3.1:8b
# OLLAMA_CODE_MODEL=codestral:22b
# OLLAMA_FINANCE_MODEL=llama3.1:70b
#
# Fine-tuned Spyder variant (after Phase 3 fine-tuning):
# OLLAMA_FAST_MODEL=gemma4-spyder:e4b
# OLLAMA_FINANCE_MODEL=gemma4-spyder:e4b

# Agent settings
OLLAMA_DEFAULT_TEMPERATURE=0.3
OLLAMA_TIMEOUT_SECONDS=120

# ==============================================================================
# MACRO & BREADTH DATA (Free — no subscription required)
# ==============================================================================

# FRED API (Federal Reserve Economic Data)
# Provides: 10Y/2Y Treasury yields, Fed Funds rate, USD broad index (DXY proxy),
#           yield curve spread (inversion signal), daily VIX close (backup)
# Register free at: https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=2fab60ad548ff78c4d3919ed2590a254

# ==============================================================================
# ADVANCED SETTINGS (Usually don't need to change)
# ==============================================================================
# System Performance
# MAX_MEMORY_USAGE_MB=1024
# HEALTH_CHECK_INTERVAL=300

# Agent veto controls (MasterController fallback defaults)
# Set to false to disable specific veto pathways.
ENABLE_X16_VETO=true
ENABLE_Y03_TRADE_VETO=true
ENABLE_Y05_VETO_CONSUMPTION=true

# ==============================================================================
# SETUP CHECKLIST
# ==============================================================================
# Before running Spyder, ensure you have:
# [ ] Created Tradier account at https://brokerage.tradier.com/
# [ ] Generated Tradier API key and noted account ID
# [ ] Obtained Massive API key and set MASSIVE_API_KEY
# [ ] Copied this file to .env and filled in your keys
# [ ] Set TRADING_MODE=sandbox
# [ ] Run validation: python SpyderQ_Scripts/validate_configuration.py
# [ ] Run config test: python config/config.py
#
# ==============================================================================
