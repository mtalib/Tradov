# Core Dependencies (Required)
# Version ranges pinned to prevent breaking changes
pandas>=2.0.0,<3.0.0
numpy>=1.26.0,<2.0.0  # pinned upper bound for stability
python-dateutil>=2.8.2,<3.0.0
pytz>=2021.3,<2026.0  # pinned upper bound for stability
sqlalchemy>=2.0.0,<3.0.0
aiofiles>=0.8.0,<1.0.0
python-dotenv>=0.19.0,<2.0.0
pyyaml>=6.0,<7.0
toml>=0.10.2  # TOML config read/write (TradovA03, TradovC17, TradovI03)
apscheduler>=3.10.0,<4.0.0  # job scheduler (TradovA_Core)
psutil>=5.9.0,<7.0.0  # process/system utilities (MasterController, FSeriesOrchestrator)
jsonschema>=4.0.0,<5.0.0  # config/schema validation (TradovA_Core Configuration)
pandas_market_calendars>=1.6.0  # market calendar for scheduler (TradovA_Core Scheduler)
watchdog>=3.0.0  # config file watching (TradovA_Core Configuration)
holidays>=0.13  # market holiday calendar (TradovA_Core Scheduler)
argon2-cffi>=21.3.0,<23.0  # pinned upper bound for stability

# Structured output validation (Pydantic v2)
pydantic>=2.0.0,<3.0.0
