+            current_session = getattr(engine, "current_session", None)
+            session_id = str(getattr(current_session, "session_id", "") or "")
+            save_fn(positions, session_id=session_id)
+
--- DIFF FOR Spyder/SpyderB_Broker/SpyderB03_PositionTracker.py ---
diff --git a/Spyder/SpyderB_Broker/SpyderB03_PositionTracker.py b/Spyder/SpyderB_Broker/SpyderB03_PositionTracker.py
index eb8906e..172cc41 100644
--- a/Spyder/SpyderB_Broker/SpyderB03_PositionTracker.py
+++ b/Spyder/SpyderB_Broker/SpyderB03_PositionTracker.py
@@ -52 +52,8 @@ class PositionTracker:
-    def __init__(self, spyder_client, event_manager=None, update_interval=1.0):
+    def __init__(
+        self,
+        spyder_client,
+        event_manager=None,
+        update_interval=1.0,
+        restore_state_on_start: bool = True,
+        persist_state_on_stop: bool = True,
+    ):
@@ -56,0 +64,2 @@ class PositionTracker:
+        self._restore_state_on_start = restore_state_on_start
+        self._persist_state_on_stop = persist_state_on_stop
@@ -142 +151,10 @@ class PositionTracker:
-        self.load_state(self._state_path)
+        if self._restore_state_on_start:
+            self.load_state(self._state_path)
+        else:
+            with self._position_lock:
+                self.positions = {}
+            try:
+                if self._state_path.exists():
+                    self._state_path.unlink()
+            except Exception as exc:
+                self.logger.warning("Failed to discard persisted tracker state %s: %s", self._state_path, exc)
@@ -155 +173,2 @@ class PositionTracker:
-        self.save_state(self._state_path)
+        if self._persist_state_on_stop:
+            self.save_state(self._state_path)
@@ -448 +467,3 @@ class PositionTracker:
-        self.save_state(self._state_path)
+        # In paper mode H05/R04 own restart truth; avoid reviving tracker JSON.
+        if self._persist_state_on_stop:
+            self.save_state(self._state_path)
@@ -666 +687,5 @@ def create_position_tracker(
-    spyder_client, greeks_calculator=None, event_manager=None
+    spyder_client,
+    greeks_calculator=None,
+    event_manager=None,
+    restore_state_on_start: bool = True,
+    persist_state_on_stop: bool = True,
@@ -674,0 +700,2 @@ def create_position_tracker(
+        restore_state_on_start: Restore persisted JSON state on start.
+        persist_state_on_stop: Persist JSON state on clean stop.
@@ -679 +706,6 @@ def create_position_tracker(
-    tracker = PositionTracker(spyder_client, event_manager)
+    tracker = PositionTracker(
+        spyder_client,
+        event_manager,
+        restore_state_on_start=restore_state_on_start,
+        persist_state_on_stop=persist_state_on_stop,
+    )
--- DIFF FOR Spyder/SpyderR_Runtime/SpyderR14_ExitMonitor.py ---
diff --git a/Spyder/SpyderR_Runtime/SpyderR14_ExitMonitor.py b/Spyder/SpyderR_Runtime/SpyderR14_ExitMonitor.py
index 2aea9b7..16aba6f 100644
--- a/Spyder/SpyderR_Runtime/SpyderR14_ExitMonitor.py
+++ b/Spyder/SpyderR_Runtime/SpyderR14_ExitMonitor.py
@@ -88,0 +89,2 @@ class ExitMonitor:
+        positions_provider: Optional callable returning authoritative runtime
+            positions for the current sweep.
@@ -97,0 +100 @@ class ExitMonitor:
+        positions_provider: Callable[[], dict[str, Any] | None] | None = None,
@@ -103,0 +107 @@ class ExitMonitor:
+        self._positions_provider = positions_provider
@@ -176,0 +181,31 @@ class ExitMonitor:
+        positions: dict[str, Any] | None = None
+        if self._positions_provider is not None:
+            try:
+                provided_positions = self._positions_provider()
+            except Exception as exc:
+                self.logger.warning(
+                    "ExitMonitor: could not resolve authoritative positions: %s", exc
+                )
+                provided_positions = None
+
+            if isinstance(provided_positions, dict):
+                positions = provided_positions
+            elif provided_positions is not None:
+                self.logger.warning(
+                    "ExitMonitor: authoritative positions provider returned %s; expected dict or None",
+                    type(provided_positions).__name__,
+                )
+
+        if positions is not None:
+            if not positions:
+                return
+
+            for symbol, raw_pos in list(positions.items()):
+                try:
+                    self._check_position(symbol, raw_pos)
+                except Exception as exc:
