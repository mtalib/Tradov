# SPYDER - Docker Compose
# Broker: Tradier (REST API — no local gateway required)
# Market Data: Databento (cloud streaming) / Tradier (testing)
#
# Add services here as needed (e.g. Prometheus, Grafana, Redis).

services: {}

version: '3.8'
