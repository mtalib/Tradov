# AI/ML Dependencies for Spyder AI Agents - Optimized for CPU
# Optimized for: Intel Core Ultra 7 155H, 32GB RAM, Intel Graphics
# Install with: pip install -r requirements_ai_optimized.txt

# Core AI/ML libraries - CPU optimized versions
ollama>=0.1.7  # For local LLM - works great on CPU
langchain>=0.1.0
langchain-community>=0.0.10
langgraph>=0.0.20

# Use CPU-optimized versions of ML libraries
scikit-learn>=1.3.0  # Excellent CPU performance
numpy<2.0  # Use stable version
scipy>=1.11.0  # Optimized linear algebra

# Lightweight alternatives
sentence-transformers>=2.2.2  # Can run smaller models on CPU

# Async and performance
aiohttp>=3.9.0
asyncio>=3.4.3
cachetools>=5.3.0

# Optional lightweight libraries
pandas>=2.0.0
numba>=0.58.0  # JIT compilation for speedup

# Monitoring
prometheus-client>=0.19.0
psutil>=5.9.0  # System monitoring

# --- SKIP THESE FOR NOW (GPU-heavy) ---
# chromadb>=0.4.22  # Vector DB - can be CPU intensive
# xgboost>=2.0.0  # Better with GPU
# transformers>=4.36.0  # Large models need GPU
# torch  # PyTorch - very large, better with GPU
# tensorflow  # TensorFlow - very large, better with GPU